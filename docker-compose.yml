services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: clenv-test
    volumes:
      - ./target/debug/clenv:/usr/local/bin/clenv:ro
    stdin_open: true
    tty: true
